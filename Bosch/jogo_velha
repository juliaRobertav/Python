##comandos##
#while para resposta
#for para posições
#while true para jogadas
#jogadores
#ganhador
#exibir tabela
#MQTT(pesquisar mais)
#não esquecer de aplicar no main

class Jogo:
        
    def __init__(self,criar_tabela, imprimir_tabela, jogo, tabela):
        self.criar_tabela()
        self.imprimir_tabela()
        self.jogo()
        self.tabela = tabela

    def criar_tabela(self):
        """
        "Função para criar o tabuleiro"
        :param tabela: matriz
        """
        tabela = [[0, 1, 2],
                 [3, 4, 5],
                 [6, 7, 8]
                 ]


    def imprimir_tabela(self):
        """
        "Função onde imprimi a tabela para jogar
        :param tabela: matriz
        """
        jogada == 0
        posicao == 1
        player_1 = "X"
        player_2 = "O"
        for players in posicao: 
            if player_1 == jogada:
                jogada1 == "❌"
                print(jogada1)
            if player_2 == jogada:
                jogada2 == "⭕"
                print(jogada2)

    #def jogo(imprimir_tabela):
