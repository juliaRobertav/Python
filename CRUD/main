from netflix import Cliente
from menu import menu

menu()

def objeto():
    nome = input("Digite o nome de usuário:")
    email = input("Digite o email de usuário:")
    plano = input("Digite o plano de usuário:")




def __init__(self, nome="", email="", plano="", tipo=""):
    self.nome = nome 
    self.email = email
    self.planos = ['basic', 'medium', 'premium']

def user():
    user = """INSERT INTO usuarios (nome, email, plano, tipo);"""
